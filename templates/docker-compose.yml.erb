version: '<%= @version %>'

<% unless @services.empty? -%>
services:
<%= YAML.dump(@services).sub(/.*?\n/, '') %>
<% end -%>
<%- unless @volumes.empty? %>
volumes:
<%= YAML.dump(@volumes).sub(/.*?\n/, '') %>
<% end -%>
<%- unless @networks.empty? %>
networks:
<%= YAML.dump(@networks).sub(/.*?\n/, '') %>
<% end -%>
<%- unless @secrets.empty? %>
secrets:
<%= YAML.dump(@secrets).sub(/.*?\n/, '') %>
<% end -%>
